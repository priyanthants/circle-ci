version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.11.1
    steps:
      - checkout
      - run: npm install
      - run: npm install typescript
      - run: npm run build       
workflows:
  version: 2
  application_deploy:
    jobs:
      - build
       
